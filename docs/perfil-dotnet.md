# Perfil .NET

Ver a tabela comparativa em `docs/perfil-node-ts.md`. Arquivos do perfil: `dotnet/` (`Directory.Build.props`,
`.editorconfig`, `nuget.config`, `ArchitectureTests.example.cs`) — `apply.sh <repo> --dotnet`. Laboratório:
`samples/orders-sample`; linhas concretas para o `AGENTS.md`: `templates/profiles/dotnet.md`.
