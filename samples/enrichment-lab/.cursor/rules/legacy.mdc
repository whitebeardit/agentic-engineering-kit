<!-- gerado por tools/build-cursor.py a partir de rules/legacy.md — não edite à mão -->
---
description: Legado (ativa ao tocar no monolito)
globs:
  - "**/erp-mono/**"
  - "**/Legacy/**"
  - "**/*.Legacy/**"
alwaysApply: false
---
# Legado (ativa ao tocar no monolito)

- **Nenhum refactor sem characterization test** cobrindo o fluxo (Verify + Bogus com seed fixo). Se não existe, a primeira task é criá-lo.
- Mudança de comportamento entra **atrás de feature flag**; o caminho antigo continua até o PO validar.
- `.verified.txt` mudou? Isso é mudança de comportamento: explique no PR, não aprove o snapshot em silêncio.
- Não "modernize" de passagem (nullable, LINQ, async) — um PR de refactor é um PR de refactor.
- Pergunte ao agente `legacy-navigator` onde a regra é calculada hoje antes de assumir.
